---
import achievementData from "../data/achievements.json";
---

{
	achievementData.map((year) => (
		<section class="dark:text-zinc-400 text-zinc-600">
			<div class="container px-4 py-2 mx-auto">
				<div class="grid gap-4 mx-4 sm:grid-cols-12">
					<div class="col-span-12 sm:col-span-3">
						<div
							class="text-center sm:text-left mb-6 before:block before:w-24 before:h-3 before:mb-3 before:rounded-md before:mx-auto sm:before:mx-0 before:dark:bg-teal-400 before:bg-slate-950">
							<h2 class="text-3xl text-zinc-900 font-bold dark:text-zinc-300">{year.year}</h2>
							<span class="text-sm font-bold tracki uppercase dark:text-gray-400">{year.description}</span>
						</div>
					</div>
					<div class="relative col-span-12 px-2 space-y-3 sm:col-span-9">
						<div
							class="col-span-12 space-y-12 relative px-3 sm:col-span-8 sm:space-y-8 sm:before:absolute sm:before:top-2 sm:before:bottom-0 sm:before:w-0.5 sm:before:-left-3 before:dark:bg-gray-700 before:bg-gray-500 p-2">
							{
								year.achievements.map((achievement) => (
									<div
										class="flex flex-col sm:relative sm:before:absolute sm:before:top-2 sm:before:w-4 sm:before:h-4 sm:before:rounded-full sm:before:left-[-31px] sm:before:z-[1] before:dark:bg-teal-400 before:bg-slate-950 bg-zinc-200 dark:bg-gray-800 rounded-3xl p-4">
										<h3 class="text-xl font-bold text-zinc-900 dark:text-zinc-100">{achievement.name} </h3>
										<div class="text-md text-zinc-700 dark:text-zinc-200">{achievement.byline}</div>
										<p class="mt-3 text-sm text-zinc-500 dark:text-zinc-300">{achievement.description}</p>
									</div>
								))
							}
						</div>
					</div>
				</div>
			</div>
		</section>
	))
}